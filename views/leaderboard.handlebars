<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Welcome to After Dark's Quoting Journal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<style>
    /* Scrollable Container */
    .scrollable-container {
        max-height: 500px;  /* Set a fixed height */
        overflow-y: auto;   /* Enable vertical scrolling */
        padding-right: 10px; /* Prevent cut-off */
    }

    .quote-card {
        transition: transform 0.2s ease-in-out;
    }
    .quote-card:hover {
        transform: scale(1.05);
    }

    /* Custom Scrollbar Styling */
    .scrollable-container::-webkit-scrollbar {
        width: 8px;
    }
    .scrollable-container::-webkit-scrollbar-thumb {
        background-color: #888;
        border-radius: 5px;
    }
    .scrollable-container::-webkit-scrollbar-thumb:hover {
        background-color: #555;
    }
</style>


<body class="bg-light">
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow-lg p-4" style="max-width: 800px; width: 100%;">
            <h1 class="text-center">Quote Leaderboards 📺</h1>

            <div class="mb-3 mt-3">
                <input type="text" id="search-bar" class="form-control" placeholder="Search quotes...">
            </div>

            <!-- Scrollable Section -->
            <div class="scrollable-container border rounded p-3 shadow-sm" style="height:600px;">
                <div class="row row-cols-1 row-cols-md-2 row-cols-lg-2 g-3" id="quotes-container">
                    {{#each quotes}}
                    <div class="col quote-card-container">
                        <a href="/quote/{{this.message_id}}" class="text-decoration-none">
                            <div class="card quote-card h-100 shadow-sm">
                                <div class="card-body text-center">
                                    <p class="card-text fw-bold quote-content">"{{this.content}}"</p>
                                    <p class="text-muted small">- {{this.user}}</p>
                                </div>
                            </div>
                        </a>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.getElementById('search-bar').addEventListener('input', function() {
            let searchQuery = this.value.toLowerCase();
            let quotes = document.querySelectorAll('.quote-card-container');

            quotes.forEach(quote => {
                let content = quote.querySelector('.quote-content').textContent.toLowerCase();
                if (content.includes(searchQuery)) {
                    quote.style.display = "block";
                } else {
                    quote.style.display = "none";
                }
            });
        });
    </script>
</body>
</html>