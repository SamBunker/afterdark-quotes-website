<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Limbo Quote Review</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow-lg p-4" style="max-width: 600px; width: 100%;">
            <h1 class="text-center mb-4">Limbo Quote Review</h1>
            
            {{#each messages}}
            <div class="border rounded p-3 mb-3 bg-white">
                    {{!-- <p class="text-muted text-center">Debug ID: {{message_id}}</p> --}}
                    {{!-- <p><strong>User:</strong> {{user}} <br> --}}
                   <p class="text-center h2 p-3" style="text-shadow: 2px 2px 3px #00000046;">{{content}}</p><br>
                   <p class="text-center h6 p-3">Date of Message: <span class="format-date">{{timestamp}}</span>
                </p>
                <form action="/quote/{{message_id}}" method="POST" class="d-flex gap-2">
                    <button type="submit" name="action" value="yes" class="btn btn-success flex-fill">✅ Yes, this is a quote</button>
                    <button type="submit" name="action" value="no" class="btn btn-danger flex-fill">❌ No, this is not a quote</button>
                </form>
            </div>
            {{else}}
            <p class="text-center text-muted">No messages available to review.</p>
            {{/each}}

            <!-- Footer Disclaimer -->
            <div class="mt-4 pt-3 border-top">
                <div class="alert alert-warning mb-0" role="alert">
                    <small><strong>Disclaimer:</strong> These quotes are collected for entertainment purposes only and do not represent the actual beliefs or opinions of the individuals mentioned. Content should not be shared outside of this platform.</small>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/dateUtils.js"></script>
</body>
</html>
{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages Review</title>
</head>
<body>
    <h1>Review Messages</h1>
    {{#each messages}}
    <div>
        <p><strong>User:</strong> {{user}} <br><strong>Timestamp:</strong> {{timestamp}}<br><strong>Message:</strong> {{content}}</p>
        <form action="/quote/{{message_id}}" method="POST">
            <button name="action" value="yes" class="btn btn-primary">Yes, this is a quote</button>
            <button name="action" value="no" class="btn btn-danger">No, this is not a quote</button>
        </form>
    </div>
    {{else}}
    <p>No messages available to review.</p>
    {{/each}}
</body>
</html> --}}