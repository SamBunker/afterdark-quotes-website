<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Welcome to After Dark's Quoting Journal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="card shadow-lg p-4" style="max-width: 800px; width: 100%;">
            <h1 class="text-center mb-4">Who Are You?</h1>

            <div class="border rounded p-3 mb-3 bg-white">
                <div class="row g-2">
                    {{#each names}}
                    <div class="col-4 d-flex">
                        <form action="/identity" method="post" class="flex-grow-1">
                            <input type="hidden" name="nickname" value="{{this.nickname}}">
                            <button class="btn btn-primary w-100 text-truncate" type="submit" name="userId" value="{{this.id}}">
                                {{this.nickname}}
                            </button>
                        </form>
                    </div>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>